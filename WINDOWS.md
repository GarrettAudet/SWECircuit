# Windows Checkout

SWECircuit release evidence includes paths longer than the legacy Windows limit.
Enable Git long-path support before cloning:

```powershell
git config --global core.longpaths true
```
