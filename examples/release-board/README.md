# Release Board

A dependency-free local app for tracking release checks, blockers, owners, and evidence.

```powershell
cd examples/release-board
npm.cmd test
npm.cmd start
```

Open `http://127.0.0.1:4174`. Data stays in browser local storage; the app makes no network
requests.
