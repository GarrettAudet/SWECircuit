# Minimal Example

This directory is a valid zero-artifact SWECircuit project plus a two-event caller-owned trace. It exists to make the repository quick start repeatable.

The project manifest is `swecircuit.json`. The trace at `traces/example.jsonl` records one explicit run start and completion; inspection does not require the trace to be declared by the project.
