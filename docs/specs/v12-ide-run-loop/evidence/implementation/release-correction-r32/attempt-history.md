# Revision 32 Attempt History

## Attempt 1

- Input outcome: verified independent `fix` from Revision 31 review.
- Main integration owner: applied the bounded cross-boundary batch correction under standing owner authorization; no specialist runtime was launched for implementation.
- Native `apply_patch` could add new files but could not refresh existing files in the Windows recovery worktree. Existing-file edits used exact SHA-256 preconditions, unique anchors, UTF-8 without BOM, immediate diff inspection, and repository formatting.
- Sandboxed lifecycle termination returned a host-denied `taskkill` result; the identical out-of-sandbox test passed.
- Sandboxed gate materialization returned `EPERM` under `.local`; the identical declared-workspace run passed.
- Focused source outcome: `pass`.
- Release outcome: pending exact aggregate and fresh package-bound review; no successor freeze is approved.
