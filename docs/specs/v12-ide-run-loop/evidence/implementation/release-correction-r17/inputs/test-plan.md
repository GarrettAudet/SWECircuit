# Revision 17 Test Plan

## Required Checks

1. Authenticate every declared source against its exact byte count and SHA-256 digest before editing.
2. Prove the current harness and verifier contain no static or pre-authentication import of repository-live `dist/index.js`.
3. Prove candidate harness/verifier blob authentication occurs before runtime materialization and import.
4. Prove the runtime source closure is materialized only from exact candidate Git blobs.
5. Prove the dependency closure is derived from candidate `package-lock.json`, package versions match, every copied entry is regular, and the private materialization supplies runtime resolution.
6. Prove the complete generated `dist` closure is hashed before dynamic import and included in stable `reviewTooling` identity.
7. Prove prepare, compile, approve, and handoff verification reconstruct and compare the same candidate-runtime binding.
8. Add mutation coverage for live `dist`, candidate source, lockfile/package supply, generated output, unsafe entries, and incomplete bindings; each relevant substitution must fail closed or be demonstrably irrelevant.
9. Run `node --test test/v12-release-review.test.mjs test/v12-release-gate.test.mjs`.
10. Run `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-template.ps1`.
11. Run repository format, lint, typecheck, build, and `git diff --check`; inspect the complete diff for scope and trust-boundary accuracy.

## Success

All focused checks pass; no release-review process executes repository-live generated code; every semantic decision is made by a private candidate-derived runtime whose source, dependency supply, toolchain facts, generated closure, and exact candidate identity are bound and reconstructed.

## Stop

Return `fix`, `diagnose`, or `block` if source authentication fails, the dependency closure cannot be proven from the lockfile, a module can resolve outside the private supply, generated bytes execute before binding, cleanup can escape its dedicated root, any required check fails causally, or the correction requires edits outside declared scope.
