# Revision 41 Correction Contract

## Trigger

Revision 40 passed its exact one-shot gate. Fresh three-domain R2 then returned two `pass` outcomes and one verified security `fix`.

## Objective

Close the canonical gate's ambient execution authority and make the independent-review package cover every transitive security-causal helper and fixture.

## Scope

- `scripts/run-v12-release-gate.mjs`
- `docs/specs/v12-ide-run-loop/evidence/release-review-r2/run-release-review.mjs`
- Release-gate and release-review regressions
- Exact lifecycle production-identity pins
- Candidate 17 retirement, feature status, milestone, and durable memory

Product APIs, schemas, specialist compilation, IDE Run Loop behavior, provider selection, spawning, persistence, integration, and merge effects are out of scope.

## Required Behavior

1. The gate effective environment is an exact allowlist with no ambient Node, npm, Git, or secret channels.
2. Node, npm, Git, shell, TypeScript, and host dependency bytes are explicit receipt authority and remain stable before/after execution.
3. npm runs offline with lifecycle scripts disabled and two private empty configuration files.
4. The R2 consumer requires and validates receipt v1alpha2.
5. All six omitted transitive sources appear exactly once with security review ownership.
6. Historical context-deduplication behavior remains covered independently.
7. Revision 41 passes focused checks, the complete release-specific suite, frozen verification, one exact gate, fresh R2, hosted CI, closeout, and merge.

## Route

`review -> fix -> verify`
