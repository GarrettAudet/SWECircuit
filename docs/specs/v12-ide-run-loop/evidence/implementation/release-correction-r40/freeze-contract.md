# Revision 40 Freeze Contract

## Included Change

- Complete package-marker classification in the R2 candidate harness.
- Focused regression coverage for complete, diagnostic-only, and partial roots.
- Exact lifecycle production-identity refresh.
- Candidate 16 gate/R2 retirement evidence, live trace updates, and durable memory.

No product API, schema, package contract, specialist runtime, canonical gate, or public behavior changes.

## Pre-Commit Gate

- Format, lint, typecheck, and template checks pass.
- Focused lineage and identity checks pass 2/2.
- The exact prior tree resolves to 22 contiguous package-backed revisions.
- Concurrent release suites pass 50/50.
- Status anti-drift checks pass 2/2.
- Diff and scope review pass.

The mutable full verifier is expected to reject copied lifecycle when committed Revision 39 bytes are compared with working Revision 40 identity pins. That mixed-identity attempt is invalid evidence, not a source result.

## Post-Commit Gate

After freezing one exact Revision 40 commit:

1. Run the complete repository verifier against the clean committed identity.
2. Preserve its exact result; do not reinterpret a failure.
3. On `pass`, run the canonical release gate exactly once for that commit.
4. On canonical `pass`, prepare, compile, approve, launch, and verify fresh three-domain R2 review.
5. Require hosted CI, milestone closeout, and owner merge evidence.

Never rerun a consumed canonical-gate source.
