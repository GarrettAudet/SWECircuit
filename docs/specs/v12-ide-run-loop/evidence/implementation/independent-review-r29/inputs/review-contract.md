# Revision 29 Independent Review Contract

## Decision

Independently determine whether exact Revision 29 closes the Revision 28 copied-production TypeScript execution finding without weakening candidate, package, lifecycle, specialist, evidence, cleanup, or host-boundary guarantees.

## Required Review Points

1. Authenticate every declared snapshot, including the immutable checkpoint, raw aggregate log, and exact source delta, before semantic review.
2. Prove the copied-production lifecycle invokes the production `scripts/run-typescript.mjs` boundary and performs a real bounded TypeScript compile rather than a syntax-only substitute.
3. Trace the exact external adapter and complete version-bearing TypeScript binding through the copied candidate; require one success-only compile sentinel in canonical raw evidence.
4. Confirm a failed compile cannot emit success, malformed or duplicated receipt/sentinel evidence fails closed, and persistent compiler mutation after child return reaches the required failing route.
5. Confirm the adapter remains single-link, external to the copied package, and host-supplied without widening provider-neutral kernel claims or trusting ambient PATH fallback.
6. Confirm exact materialization, nested Git isolation, immutable source identity, package closure, negative routes, cleanup, and Candidate 13 non-consumption remain closed.
7. Authenticate the final V11 Revision 43 Candidate A/Audit B pair, LF-stable receipt, 6,600-byte binder, 8,713-byte semantic pass, and exact authorization. Treat earlier authority-violating and normalization-stale handoffs only as preserved history.
8. Authenticate the complete aggregate log and verify 439/439 core tests, the exact 1/1 copied lifecycle, V10/V11/V12 dogfood, package inspection, and installed-consumer gate.
9. Treat owner verification and receipts as evidence only. Reproduce static claims from snapshots and search for a concrete bypass before returning `pass`.
10. Return `pass` only when every point is resolved and no release-blocking defect remains; otherwise return the smallest causal non-pass route.

Remain read-only. Do not rerun any retired, current, or successor candidate gate, edit files, install packages, use network access, approve release or merge, or claim host enforcement. Return only the exact generated `SpecialistAgentHandoff` JSON object.
