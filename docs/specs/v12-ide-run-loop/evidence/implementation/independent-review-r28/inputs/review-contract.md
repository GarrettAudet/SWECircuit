# Revision 28 Independent Review Contract

## Decision

Independently determine whether exact Revision 28 closes the Revision 27 execution-binding findings without weakening candidate, package, lifecycle, specialist, evidence, cleanup, or host-boundary guarantees.

## Required Review Points

1. Authenticate every declared snapshot, including the checkpoint, raw verification logs, and source delta, before semantic review.
2. Prove that the packed-consumer path preserves the complete authenticated TypeScript binding and passes that exact object into `executeTypeScript`; reject any reduction to a path-only capability.
3. Trace pre-spawn and post-spawn path, byte-count, digest, link-count, and observed-version authentication for both version inspection and compilation.
4. Confirm hostile PATH tests install platform-selectable commands and prove ambient `tsc` cannot win on Windows or POSIX.
5. Confirm persistent compiler mutation, path substitution, aliases, junctions/symlinks, hard links, candidate containment, and dependency fallback all fail closed through exported production paths.
6. Verify the documented boundary distinguishes kernel entrypoint authentication from host-enforced concurrent-writer isolation, transitive toolchain trust, process isolation, and runtime supply.
7. Confirm copied-production identities, exact materialization, package metadata, hosted workflow supply, installed-consumer behavior, and cleanup remain closed.
8. Authenticate the complete V11 Revision 42 Candidate A/Audit B replay, explicit receipt, binder handoff, dependency assessment, semantic pass, and cross-package authorization; use the Revision 41 annotation only as preserved history.
9. Assess the opaque first aggregate stop against the exact no-change core/lifecycle replays and fully preserved passing aggregate log. Do not invent a source cause unsupported by retained evidence.
10. Treat owner verification and receipts as evidence only. Reproduce static claims from snapshots and search for a concrete new bypass before returning `pass`.

## Verdict

Return `pass` only when every required point is resolved with source-cited evidence and no release-blocking defect remains. Otherwise return `fix`, `diagnose`, `clarify`, `redesign`, `split`, or `block` with the smallest causal next action.

Remain read-only. Do not rerun any retired or successor candidate gate, edit files, install packages, use network access, approve release or merge, or claim host enforcement. Return only the exact generated `SpecialistAgentHandoff` JSON object.
