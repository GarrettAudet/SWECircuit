# Revision 27 Independent Review Contract

## Decision

Independently determine whether exact Revision 27 causally closes the authenticated TypeScript execution defects reported by Revision 26 without weakening release, package, lifecycle, specialist, evidence, or cleanup controls.

## Required Review Points

1. Authenticate every declared snapshot before semantic review.
2. Trace package build, typecheck, canonical release-gate, and packed-consumer compilation to one explicitly authenticated TypeScript file rather than ambient command lookup.
3. Assess case-insensitive duplicate supply, empty and relative paths, missing targets, directories, candidate containment, final and ancestor aliases, dependency-directory fallback aliases, and hard-link handling.
4. Trace canonical path, byte count, digest, link count, origin, and observed version from resolution through the exact file executed for version inspection and compilation.
5. Search for mutation windows before, during, and after version inspection and compilation, including whether post-execution reauthentication can be bypassed.
6. Evaluate whether hostile tests use distinguishable sentinels and exercise real exported production paths rather than test-only copies.
7. Confirm package metadata, formatter scope, copied-production identities, exact lifecycle materialization, installed-package consumer behavior, and cleanup remain closed.
8. Authenticate the Revision 41 V11 replay evidence, including the observed block when a runtime input was omitted and the subsequent package-bound pass route.
9. Treat owner verification and aggregate receipts as evidence only. Reproduce static claims from snapshots and search for a new concrete bypass before returning pass.

## Verdict

Return `pass` only when every required point is resolved with source-cited evidence and no release-blocking defect remains. Otherwise return `fix`, `diagnose`, `clarify`, `redesign`, `split`, or `block` with the smallest causal next action.

Remain read-only. Do not rerun any retired or successor candidate gate, edit files, install packages, use network access, approve release or merge, or claim host enforcement. Return only the exact generated `SpecialistAgentHandoff` JSON object.
