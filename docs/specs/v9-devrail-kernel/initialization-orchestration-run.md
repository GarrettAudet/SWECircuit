# V9 Initialization Orchestration Run

## Status

In progress.

## Pattern

Single implementation owner plus one independent read-only reviewer.

## Branch And State

- Branch: `codex/v9-devrail-kernel`
- Frozen input baseline: `3e0b698`
- Active task: T007 project initialization
- Merge target: none for the reviewer handoff

## Roster

| Role | Work Unit | Authority | Stop Condition |
| --- | --- | --- | --- |
| Primary IDE agent | A: integrated implementation | Own source, tests, diagnosis, synthesis, and verification. | Stop on overwrite risk, ambiguous ownership, or scope expansion. |
| Parfit (`019f5cec-0ed5-7b72-9776-a3977b2117e3`) | B: independent initialization review | Read and recommend only. | Stop outside T007 initialization. |

## Integration Notes

- The reviewer receives no write authority, so source and test ownership remain unambiguous.
- Implementation can continue while the reviewer examines the recorded contract and current bounded changes.
- Findings must map to fixed, deferred, or rejected-with-rationale before T007 passes.

## Handoff

Initial verdict: `REVISE`.

| Finding | Disposition | Evidence |
| --- | --- | --- |
| Root and catalog replacement races were not bounded. | Fixed with root, parent, created-entry, and descriptor identity/canonical checks before and after each mutation; the remaining pure-Node sub-syscall window is documented as deferred native boundary X07. | `src/initialize.ts`; v1alpha1 Initialization contract |
| Success and rollback did not prove ownership of every directory. | Fixed with a BigInt device/inode journal, post-validation checks, reverse non-recursive identity-gated cleanup, and `SC1022` when cleanup cannot be proven safe. | `src/initialize.ts`; initialization recovery tests |
| Descriptor close and recovery could throw; collision and cleanup diagnostics were inconsistent. | Fixed with guarded close, catch-all operation results, exit-class-4 `SC1021`, broader `SC1001`, and dedicated `SC1022`. | `src/diagnostics.ts`; diagnostic catalog; checkpoint tests |
| Project-ID derivation and generated bytes were underspecified. | Fixed by freezing the exact transform and exact UTF-8 manifest representation in contract and tests. | v1alpha1 contract; ID and raw-byte tests |
| Race, fault, sentinel-preservation, and dynamic offline tests were missing. | Fixed with 13 deterministic checkpoints, non-empty and identity-swap recovery tests, a coordinated two-process race, and network/process API traps across initialization and validation. | `test/initialization.test.mjs`; race worker |

Focused re-review 1 verdict: `REVISE`.

| Finding | Disposition | Evidence |
| --- | --- | --- |
| The documented replacement guarantee remained too categorical across create-to-capture and check-to-remove windows. | Fixed by naming both windows in the contract and X07, qualifying categorical traversal/removal claims, and requiring a native adapter for malicious-race protection across those windows. | v1alpha1 Initialization contract; case matrix X07 |
| A successful directory creation or exclusive manifest open could fail before entering the identity journal without forcing `SC1022`. | Fixed with a pending-creation ledger. Pre-capture failures preserve the path and force `SC1022`; all four create/open boundaries have deterministic tests. | `src/initialize.ts`; pre-capture fault tests |
| The two-process test released workers before proving they had both passed collision preflight. | Fixed by placing the ready/start barrier inside `beforeCatalogCreate`, so both processes block after preflight and race on the same exclusive `mkdir`. | race worker; coordinated process test |

Focused re-review 2 verdict: `PASS`, with no actionable findings. The reviewer confirmed the two accepted race windows are explicit, every successful create/open enters the pending ledger before capture, all four pre-capture failures preserve output and emit `SC1022`, and both process workers block after preflight before the exclusive-create race.

## Verification

Canonical local verification passes with 82 tests, package inspection, and the repository template checker. Independent fan-in is `PASS`. Pending commit and remote Node 22/24 matrix across Ubuntu, Windows, and macOS.