# Milestone: V10 Bounded Executor Boundary

## Status

Candidate `d0ef8f9` passed all seven hosted jobs in run `29400365173` in 10m47s, the exact local 119-scenario matrix in 653.0 seconds, and security plus API/documentation review. Correctness returned `REVISE` because the user-facing overview called three source records that omitted the same owner (`7f30107`) "three historical matrix owners." The correction preserves the one-owner/three-source distinction. Not merged.

## Goal

Add the smallest honest execution layer above V9: one bounded work packet, one host-injected executor, one invocation-scoped authority grant, and one inspectable lifecycle journal, without claiming grant consumption or replay prevention.

## Shipped

- A provider-neutral `executeWorkPacket` library boundary for one host-selected packet.
- Closed grant, manifest, identity, permission, timeout, snapshot, privacy, and settlement gates before or around invocation.
- Deterministic V9-compatible lifecycle journals with honest cancellation, timeout, and `abort_unconfirmed` outcomes.
- A no-I/O deterministic test executor, installed embedding guide, public TypeScript types, and clean consumer proof.
- V10 dogfood evidence, diagnostics `SC4201` through `SC4210`, workflow checker coverage, research, ADR, feature records, and durable memory updates.

## Why It Matters

V10 is the first executable bridge from a validated SWECircuit work packet to a trusted IDE or agent runtime. It standardizes the handoff and evidence shape without pretending that core schedules work, enforces a sandbox, owns credentials, or controls merge.

## Source Artifacts

- Feature package: `docs/specs/v10-executor-adapter/`
- Architecture: `docs/architecture/decisions/0002-bounded-executor-boundary.md`
- Research: `docs/research/snapshots/2026-07-14-v10-executor-boundary-scan.md`
- Active-Markdown research: `docs/research/snapshots/2026-07-14-v10-commonmark-ownership-scan.md`
- Runtime guide: `docs/framework/executor-boundary.md`
- Evidence: `docs/specs/v10-executor-adapter/evidence/`
- Review run: `docs/specs/v10-executor-adapter/orchestration-run.md`

## Verification

- Candidate `57bab44` passed `npm.cmd run verify` in 17.6 seconds with format, lint, typecheck, build, 275 tests, deterministic V10 dogfood, package inspection, and the clean offline installed consumer.
- Dogfood proved an `SC4206` rejection with zero executor calls, a corrected single invocation, a seven-event inspectable journal, stable bytes, and identity-checked cleanup.
- The first three-way postimplementation review returned `REVISE` from all reviewers. Exact review of `e3453e0` then returned one `PASS` and two `REVISE` verdicts; settlement detachment and no-call documentation now have causal fixes and regression coverage.
- Candidate `9d8907a` received three exact `PASS` verdicts and passed all seven jobs in GitHub Actions run `29357443883`.
- Candidate `2c6dff4` passed all seven jobs in GitHub Actions run `29358105210`, but exact review returned correctness `PASS`, security `REVISE`, and API/documentation `REVISE` for two synonymous stale claims.
- Candidate `dbbeeb1` passed all seven jobs in GitHub Actions run `29358867851`, but exact review returned correctness `REVISE`, security `REVISE`, and API/documentation `PASS`.
- Candidate `4c6818d` passed all seven jobs in GitHub Actions run `29359564312`; exact review returned correctness `REVISE`, security `PASS`, and API/documentation `REVISE`.
- Candidate `b2d73e7` passed all seven jobs in GitHub Actions run `29361203381`; exact review returned `REVISE` from correctness, security, and API/documentation.
- Candidate `ac70efc` passed all seven jobs in GitHub Actions run `29364033724`; exact review returned correctness `PASS`, security `REVISE`, and API/documentation `REVISE`.
- Candidate `9209ff1` passed all seven jobs in GitHub Actions run `29366578213`; exact review returned correctness `PASS`, security `REVISE`, and API/documentation `PASS`.
- Candidate `b3ff0d3` passed all seven jobs in GitHub Actions run `29370427573`; exact review returned correctness `REVISE`, security `REVISE`, and API/documentation `PASS`.
- Candidate `394612d` returned correctness `REVISE`, security `REVISE`, and API/documentation `REVISE` for list-contained fences, legacy raw structural checks, presence-only required headings, and overstated docs. GitHub Actions run `29372879405` passed six kernel-toolchain jobs but failed Template Check on a host-newline-dependent fixture.
- Candidate `0c42c64` passed Template Check and all six kernel-toolchain jobs in GitHub Actions run `29375642610`; correctness, security, and API/documentation all returned `REVISE`.
- Candidate `7f02b87` passed Template Check and all six kernel-toolchain jobs in GitHub Actions run `29377581706`; correctness, security, and API/documentation all returned `REVISE` for missing fence-container identity and list continuation state.
- Candidate `c4bfa01` passed Template Check and all six kernel-toolchain jobs in GitHub Actions run `29380939276`; correctness, security, and API/documentation all returned `REVISE`.
- Candidate `ae5195c` passed Template Check and all six kernel-toolchain jobs in GitHub Actions run `29383056180` in 7m05s; API/documentation returned `PASS`, correctness returned `REVISE`, and security produced no verdict within the bounded handoff window.
- Candidate `f990abc` passed Template Check and all six kernel-toolchain jobs in GitHub Actions run `29384351025` in 6m25s; correctness, security, and API/documentation each returned `REVISE`.
- Candidate `0f952d9` passed Template Check and all six kernel-toolchain jobs in GitHub Actions run `29386833535` in 9m19s; correctness returned `PASS`, while security and API/documentation returned `REVISE`.
- Candidate `f779cab` passed Template Check and all six kernel-toolchain jobs in GitHub Actions run `29388623286` in 9m20s; API/documentation returned `PASS`, while correctness and security returned `REVISE`.
- Candidate `82c3bb1` passed Template Check and all six kernel-toolchain jobs in GitHub Actions run `29390051639` in 10m21s; correctness and API/documentation returned `PASS`, while security returned `REVISE`.
- Candidate `dd575d5` passed Template Check and all six kernel-toolchain jobs in GitHub Actions run `29391822367` in 9m39s; correctness and API/documentation returned `PASS`, while security returned `REVISE`.
- Candidate `49b22ba` passed Template Check and all six kernel-toolchain jobs in GitHub Actions run `29393468684` in 11m56s; correctness, security, and API/documentation returned `REVISE`.
- Candidate `7f30107` passed Template Check and all six kernel-toolchain jobs in GitHub Actions run `29395470172` in 11m39s; correctness and security returned `PASS`, while API/documentation returned `REVISE` for two stale review-event dates. Template Check took 11m34s.
- Candidate `57bab44` passed Template Check and all six kernel-toolchain jobs in GitHub Actions run `29396782369` in 11m35s; security returned `PASS`, while correctness and API/documentation returned `REVISE` for inherited-evidence attribution. Template Check took 11m30s.
- Candidate `e5f870e` passed Template Check and all six kernel-toolchain jobs in GitHub Actions run `29398170073` in 12m29s; correctness returned `PASS`, while security and API/documentation returned `REVISE` for historical evidence ownership and adapter taxonomy. Template Check took 12m25s.
- Candidate `d0ef8f9` passed Template Check and all six kernel-toolchain jobs in GitHub Actions run `29400365173` in 10m47s; security and API/documentation returned `PASS`, while correctness returned `REVISE` for the milestone's evidence-cardinality wording. Template Check took 10m44s.
- Candidate `7f30107` owns its complete local Windows matrix in 576.2 seconds. Candidate `d0ef8f9` owns a separate exact local 119-scenario matrix in 653.0 seconds and its exact hosted 119-scenario Template Check in 10m44s, but semantic review rejected it. A successor must pass its own exact review plus hosted CI before owner approval.

## Approval Gate

- Source branch: `codex/v10-executor-adapter`
- Target branch: `main`
- Current state: `d0ef8f9` is rejected despite exact local and hosted checks; a successor must preserve one-owner/three-source evidence cardinality and pass exact-commit review plus hosted CI
- Required decision: owner approval only after exact-commit checks and independent `PASS` verdicts are recorded
- Merge action: prohibited before the approval gate is satisfied

## Residual Risks

- The embedding host, not SWECircuit, must sandbox executor code and enforce the checked grant.
- A blocking in-process executor can stall timers and cancellation; unacknowledged aborts remain potentially live.
- V10 has no scheduler, retry engine, provider loader, durable writer, merge automation, or automatic memory mutation.
- The 0.x API may change after real adapter use; public reuse remains legally unclear without a license.
- The active-Markdown checker is bounded defense in depth, not a full CommonMark renderer; parser replacement or contract narrowing remains separately scoped.

## Next Recommended Work

- Run the acceptance gate against the exact commit containing the complete state: correctness, security, API/documentation, and hosted CI.
- Obtain explicit exact-commit `PASS` verdicts from correctness, security, and API/documentation review.
- Record the evidence-only closeout, then request owner approval to merge to `main`.
- After V10 adoption, build the smallest provider-independent orchestration planner that turns a goal and module circuit into dependency-bounded work packets, matches required capabilities to available specialized agents, schedules safe fan-out and fan-in, and emits an integration plan plus trace.
- Keep IDE, model, and provider integrations as replaceable execution adapters; they expose capabilities and execute assigned packets but do not own decomposition or routing policy. Optional policy compilers may implement that SWECircuit-owned policy only by emitting portable framework contracts.

## User-Facing Overview

V10 executes one validated work packet through a trusted host-injected executor with explicit grant checks and a deterministic lifecycle trace. The runtime remains green. Candidate `d0ef8f9` passed the exact local matrix, all seven hosted jobs, and security plus API/documentation review, but correctness found one milestone sentence that turned three source records that omitted the same owner (`7f30107`) into three owners. The correction preserves that cardinality; exact review, all seven hosted jobs, and owner approval remain before merge. After V10, the next product slice is provider-independent decomposition, capability matching, and parallel orchestration.
