# Milestone: V8.1 Baseline Integrity

## Status

Complete.

## Goal

Make TraceRail's manual baseline honest, internally consistent, checker-enforced, and ready to support the V9 portable kernel.

## Shipped

- Repositioned TraceRail as a checked operating protocol and labeled the overview as the target model.
- Normalized the module template and all five rail-local module tables.
- Enforced canonical workflow outcomes and separated governance statuses.
- Added dynamic module, rail, and official-pack discovery.
- Fixed completed-debug validation and decimal milestone handling.
- Added source provenance to 28 decisions and 36 named patterns.
- Added a permanent fifteen-case positive/negative checker suite to local guidance and CI.
- Made expected child-checker failures portable across PowerShell editions and updated actions/checkout to the current official v7 release.
- Completed the first write-enabled dogfood run with two successful worker handoffs, two failed attempts, no scope conflicts, and centralized recovery.

## Why It Matters

TraceRail now tells the truth about what exists, enforces its own manual contracts more deeply, and preserves real evidence about where parallel development succeeds and fails. V9 can build on a coherent baseline instead of contradictory prose.

## Source Artifacts

- Feature package: docs/specs/v8.1-baseline-integrity/
- Orchestration run: docs/specs/v8.1-baseline-integrity/orchestration-run.md
- Review: docs/specs/v8.1-baseline-integrity/review.md
- Regression suite: scripts/test-check-template.ps1

## Verification

- Positive template checker passed.
- Fifteen-case checker regression suite passed.
- PowerShell parser, links, outcomes, provenance, placeholders, ASCII, and whitespace checks passed.
- Main CI run 29069685873 passed every workflow step after the fast-forward merge.

## Approval Gate

- Source branch: codex/v8.1-baseline-integrity
- Target branch: main
- Current state: Merged into `main` at `fa2a73d`; main CI is green.
- Required decision: Complete for merge. No license selection was provided, so no licensing change is authorized.
- Merge action after approval: Completed. Start V9 from the verified `main` baseline.

## Residual Risks

- No executable kernel, router, worktree manager, or merge engine exists yet.
- Only two of four worker attempts completed successfully.
- Full transcript and raw command capture remain incomplete.
- License selection remains owner-controlled.

## Next Recommended Work

Build V9 as the portable schema, validator, and project-initializer kernel. Include runtime heartbeats, deadlines, cancellation, raw handoffs, and measurable dogfood metrics in its architecture.

## User-Facing Overview

V8.1 turns TraceRail into an honest and internally consistent manual baseline. Its contracts are standardized, malformed workflows are rejected by permanent regression tests, memory has source provenance, and the first write-enabled dogfood run preserves both successful handoffs and failed worker recovery.
