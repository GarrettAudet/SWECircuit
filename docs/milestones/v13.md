# Milestone: V13 Dogfood Validation

## Status

Complete. Ready for owner review on `codex/v13-dogfood-validation`.

## Goal

Use released V12 from one ordinary product request to build, verify, and review a small application through dependency-safe native IDE specialists.

## Shipped

- A polished dependency-free local issue-triage board with CRUD, status movement, filters, local persistence, and JSON import/export.
- Four task-specific compiled specialists with two parallel roots, gated integration, and independent review.
- Exact raw handoffs, correction routing, browser evidence, and an integration-ready immutable run.
- A measured dogfood assessment separating product success from framework ergonomics.

## Why It Matters

V13 proves that the released contracts can prevent scope conflicts, reject invalid evidence, route defects to the correct owner, and converge on an independently reviewed product. It also proves that the current host workflow is too manual for a project this small.

## Source Artifacts

- Feature package: `docs/specs/v13-dogfood-validation/`
- Runnable app: `examples/triage-board/`
- Browser evidence: `docs/specs/v13-dogfood-validation/evidence/browser/`
- Final review: `docs/specs/v13-dogfood-validation/review.md`
- Final run inspection: `docs/specs/v13-dogfood-validation/evidence/orchestration/run/inspection.json`
- Memory updates: `docs/memory/active-context.md`, `history-ledger.md`, `retrieval-index.md`, `known-issues.md`, and `patterns.md`

## Verification

- Application tests: 13/13 pass.
- JavaScript syntax: pass.
- Browser CRUD, persistence, search/filter, export, focus, desktop/mobile, and console checks: pass.
- Template checker: pass.
- Independent review: pass.
- Final run: `specialistOutcome: pass`, `integrationReady: true`.
- Post-memory replay: rejected a changed bound specification, restored its exact approved bytes, and passed with the original digest pair and session.
- Canonical repository verification: pass after preserving the exact V12 Revision 73 baseline in Active Context.

## Approval Gate

- Source branch: `codex/v13-dogfood-validation`
- Target branch: `main`
- Current state: verified dogfood candidate ready to commit and push
- Required decision: owner approval before merge to `main`
- Merge action after approval: fast-forward the approved branch and confirm hosted Windows CI

## Residual Risks

- Browser file selection for JSON import was blocked by host policy; deterministic tests and source review cover the behavior.
- Host lifecycle commands and browser evidence delivery remain manual.
- Model and reasoning-effort assignments were manually authored; V13 does not implement or prove least-sufficient runtime routing.
- No V12 kernel or host adapter automation was changed in this milestone.
- spec.md intentionally preserves its launch-time Active status and unchecked criteria as an immutable approved input; this milestone is the authoritative completion record.

## Next Recommended Work

Build V14 as an adaptive IDE execution layer: a quick rail for small tasks, deterministic least-sufficient model and effort routing, automatic lifecycle/evidence handling, explicit reviewer evidence bundles, and a compact live RunView. Keep the exact specialist compiler for work where parallelism or independent evidence justifies it.

## User-Facing Overview

V13 successfully used V12 to build and independently verify a real app. Parallel ownership and quality gates worked; no write conflicts occurred, four material defects were caught and fixed, and the final trace is complete. The main gap is now clear: the system needs to make this level of rigor much faster and less manual for small work.