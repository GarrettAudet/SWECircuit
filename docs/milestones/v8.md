# Milestone: V8 README Concept Visual

## Status

Ready for approval.

## Goal

Make the README visual explanation clear enough to pitch TraceRail quickly. Replace the GIF-first story with a static concept visual that teaches the core model before internal framework vocabulary.

## Shipped

- Added `docs/assets/tracerail-concept.png` as the primary README visual.
- Updated the deterministic generator to produce the concept PNG and supporting GIFs.
- Rewrote the README visual section around `goal | module | gate | evidence | memory`.
- Updated checker coverage so the README must embed the concept visual.
- Recorded the V7 clarity failure and V8 comprehension-first rule in feature, review, asset, and memory artifacts.

## Why It Matters

TraceRail should feel simple before it feels powerful. V8 gives the README a clearer LangChain-like mental model: compose workflow parts into a rail, then scale only when work units have contracts and evidence.

## Source Artifacts

- Feature package: `docs/specs/v8-readme-visual-clarity/`
- Review: `docs/specs/v8-readme-visual-clarity/review.md`
- Research snapshot: Not applicable; this version responds to direct product/README feedback.
- Memory updates: `docs/memory/active-context.md`, `docs/memory/history-ledger.md`, `docs/memory/retrieval-index.md`, `docs/memory/patterns.md`, `docs/memory/known-issues.md`

## Verification

- Commands: Python compile, asset generation, template checker, placeholder scan, non-ASCII scan, `git diff --check`.
- Manual review: In-app preview of `docs/assets/tracerail-concept.png` and README section review.

## Approval Gate

- Source branch: `codex/v8-readme-visual-clarity`
- Target branch: `main`
- Current state: Local validation complete; ready to push for user review.
- Required decision: User approves the concept visual and README direction.
- Merge action after approval: Merge V8 into `main`, then use V8 as the baseline for the next TraceRail version.

## Residual Risks

- GitHub README rendering may reveal small spacing or scale issues not visible in local preview.
- The support GIFs may become unnecessary if the static concept visual fully replaces them.
- The generator filename still says GIFs even though it now creates a PNG too.

## Next Recommended Work

- Review the V8 README on GitHub after push.
- If accepted, merge V8 to `main`.
- Consider a future asset-pipeline cleanup if README visuals continue evolving.

## User-Facing Overview

V8 changes the README from a GIF demo sequence to a clearer concept-first visualization. The new visual explains TraceRail as `goal | module | gate | evidence | memory`, then shows modules, rails, and bounded subagent scale underneath. The point is to make the framework instantly legible before asking readers to learn the vocabulary.